---
description: Run a prompt through ReasonKit's reasoning pipeline using Claude itself.
---
Run the request below through ReasonKit (installed via `pip install reasonkit`),
using Claude Code as the model behind it. The script calls the wrapped pipeline
and prints the improved answer.

Prompt: $ARGUMENTS

Run: `${CLAUDE_PLUGIN_ROOT}/scripts/reasonkit_run.py` with the prompt above.
